@model  List<Test.ViewModels.EmployeeViewModel>

<div class="jumbotron">
    @foreach (var employee in Model)
    {
        <p>@employee.Namn</p>
        //<p>@Html.DisplayFor(x => x.Namn)</p>
    }
</div>